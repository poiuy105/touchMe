<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 悬浮窗：显示常驻悬浮窗（TYPE_APPLICATION_OVERLAY） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务：保证 App 在抓取期间不被杀 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 唤醒锁：响铃时保持 CPU -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_alarm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_alarm"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaptchaAlarm">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          核心前台服务。
          exported=true 让 adb(shell uid) 能用 am start-foreground-service 启动；
          targetSdk=28 不需要声明 foregroundServiceType。
        -->
        <service
            android:name=".AlarmService"
            android:exported="true" />

    </application>

</manifest>
